<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="uk.co.ourfriendirony.medianotifier">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ActivityMain"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.ActivitySettings"/>
        <activity android:name=".activities.ActivityMovieFind"/>
        <activity android:name=".activities.ActivityTV"/>
        <activity android:name=".activities.ActivityTVFind"/>
        <activity android:name=".activities.ActivityTVNotifications"/>

        <!--<service android:name=".notifier.NotifierService">-->
        <!--<intent-filter>-->
        <!--<action android:name="uk.co.ourfriendirony.NotifierService"/>-->
        <!--</intent-filter>-->
        <!--</service>-->

        <!--<receiver-->
        <!--android:name=".notifier.NotifierReceiver"-->
        <!--android:enabled="true"-->
        <!--android:exported="true"-->
        <!--android:label="NotifierReceiver">-->
        <!--<intent-filter>-->
        <!--<action android:name="android.intent.action.BOOT_COMPLETED"/>-->
        <!--</intent-filter>-->
        <!--</receiver>-->

    </application>
</manifest>